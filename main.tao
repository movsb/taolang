function main() {
    lambda(x=>x*x);
}

function lambda(expr) {
    let a = [1,3,5,7,9];
    let n = a.length;
    let i = 0;
    while i < n {
        println(expr(a[i]));
        i = i + 1;
    }
}
