function main() {
    let a;
    let b;
    a = setTimeout(() => {
        println("AbC".lower());
        b.stop();
    }, 300);
    b = setTimeout(() => {
        println("AbC".lower());
    }, 3000);
    println(setTimeout);
}
